import modules


def test_bored():
    assert ('bored' == modules.process_query('I'm bored')[0])
    assert ('bored' == modules.process_query('I need something to do')[0])
    assert ('bored' == modules.process_query('Suggest an activity')[0])
